---- NOMINATION_ADMIN

CREATE TABLE NOMINATION_ADMIN
  (
    EVTPLACE VARCHAR2(20 BYTE) NOT NULL ENABLE,
    CUTOFDTE DATE,
    MAXTICK     VARCHAR2(20 BYTE),
    MAXPRICE    VARCHAR2(5 BYTE),
    EVTNME      VARCHAR2(100 BYTE) NOT NULL ENABLE,
    NOOFMAXTICK VARCHAR2(7 BYTE),
    INDIV_TICK  VARCHAR2(5 BYTE),
    CONSTRAINT NOMINATION_ADMIN_PK PRIMARY KEY (EVTPLACE, EVTNME)
  )


---- NOMINATION

CREATE TABLE NOMINATION
  (
    EMP_NO     VARCHAR2(8 BYTE) NOT NULL ENABLE,
    TICKTSCNT  NUMBER(5,0),
    BOARDPNT   VARCHAR2(20 BYTE),
    PAYMENTCNT NUMBER(20,2),
    ENTERDTE DATE,
    ENTERBY VARCHAR2(8 BYTE),
    MODIFYDTE DATE,
    EVTNME VARCHAR2(20 BYTE) NOT NULL ENABLE,
    FLAG   VARCHAR2(4 BYTE),
    CONSTRAINT NOMINATION_PK PRIMARY KEY (EMP_NO, EVTNME) 
  )


  Version 1.2.0 - Post AppScan Changes